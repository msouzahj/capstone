<%= form_for(@access) do |f| %>
  <% if @access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@access.errors.count, "error") %> prohibited this access from being saved:</h2>

      <ul>
      <% @access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meets_id %><br>
    <%= f.text_field :meets_id %>
  </div>
  <div class="field">
    <%= f.label :seasons_id %><br>
    <%= f.text_field :seasons_id %>
  </div>
  <div class="field">
    <%= f.label :events_id %><br>
    <%= f.text_field :events_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
